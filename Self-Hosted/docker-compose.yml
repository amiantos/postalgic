services:
  postalgic:
    build: .
    ports:
      - "8010:8010"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=8010
      # - BASIC_AUTH_USERNAME=admin
      # - BASIC_AUTH_PASSWORD=changeme
    restart: unless-stopped
